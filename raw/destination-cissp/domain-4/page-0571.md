ACK packet, and the session is established.
b. If the port is closed, the target responds with a RST
packet, and the session is terminated.
Figure 4-18: SYN Scan
Note that if the attacker wants to perform a stealth scan, e.g.,
using Nmap, they can do so by sending a RST packet at the
third step of the three-way handshake and thus never sending
the final ACK and formulating a full connection. This is known
as a stealth or half-open scan.
