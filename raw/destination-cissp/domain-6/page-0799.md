Log Event Time
Ensuring consistent time stamps of log entries is very
important. If an organization has deployed multiple servers
and other network devices—like switches and firewalls—
and each device is generating events that are logged, it’s
critical that the system time, and therefore the event log
time, for each device is the same. Otherwise, if a breach
occurs, for example, trying to correlate activities as an
