Figure 5-1: RMC Components
Logical Access Modes
 Access control is more granular than simply allowing
subjects to access objects.
 Access control rules allow the access control
mechanism to be much more granular.
 Specific access rules allow precision with regards to
what subjects can access what objects and exactly
what those subjects can do with those objects.
