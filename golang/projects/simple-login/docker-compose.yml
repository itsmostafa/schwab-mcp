version: "3"

services:
  db:
    container_name: simple-login-db
    restart: unless-stopped
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - "db:/data/db"

volumes:
  db:
